import React, {useContext} from 'react';

const Hooks = () => {

    // const {count, setCount} = useContext(0)

    return (
        <div>
            <h2>I'm calling from Hooks</h2>
        </div>
    );
}

export default Hooks;
